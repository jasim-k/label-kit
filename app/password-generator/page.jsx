import PasswordGeneratorPage from '../../components/PasswordGeneratorPage'

export const metadata = {
  title: 'Password Generator | Toolhaus',
  description: 'Generate cryptographically secure passwords with custom length, character sets, and advanced options. Runs entirely in your browser.',
}

export default function PasswordGenerator() {
  return <PasswordGeneratorPage />
}
